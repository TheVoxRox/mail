{
  "_README": [
    "Acknowledged drift for the per-subsystem security audits, checked by",
    "frontend/scripts/check-audit-freshness.mjs (runs in `npm run check`).",
    "",
    "An audit's `Audited commit` is the code state its claims were verified",
    "against. When the content under that audit's `Code paths` moves away from",
    "it, the check goes red. Two ways out, both legitimate:",
    "",
    "  1. Re-verify the audit -> bump `Audited commit` in the doc, add a change",
    "     log entry, and drop the audit's entry from this file.",
    "  2. Review the drift and conclude it cannot move a verdict -> record the",
    "     current object id of each `Code paths` entry in `reviewedTrees`, and",
    "     append a dated record to `acknowledgements` saying why.",
    "",
    "What is NOT legitimate is recording an object id without reading the diff.",
    "The record is the evidence that someone did.",
    "",
    "`reviewedTrees` holds git object ids (a tree for a directory, a blob for a",
    "file), not commit SHAs. Get them from the check itself: when it fails it",
    "prints the current id of every path that moved, ready to paste.",
    "",
    "Object ids rather than commit SHAs because this repo squash-merges: a commit",
    "SHA written during a PR stops existing when the PR lands, which used to cost",
    "a follow-up commit per acknowledgement. Squashing rewrites history, not",
    "content, so an object id survives it, along with rebases, cherry-picks and",
    "force pushes.",
    "",
    "`acknowledgements` is an ordered list of `{ date, note }`, oldest first, and",
    "it is what bounds route 2. Option 2 used to be unbounded: one note string",
    "that only ever grew, so the honest way out stayed available forever and the",
    "audit itself was never read again. Two caps now end that run and force",
    "route 1, whichever comes first:",
    "",
    "  - 8 acknowledgements. Each one judges its own delta in isolation; the",
    "    union of eight of them is not something a ninth reviewer can hold in",
    "    their head, and it is the union that decides whether a verdict moved.",
    "  - 90 days from the OLDEST record. Drift acknowledged in spring is being",
    "    defended by a judgement nobody has revisited.",
    "",
    "Both clocks reset the same way: re-verify the audit, bump `Audited commit`,",
    "and delete the entry from this file."
  ],
  "docs/UPDATER_AUDIT.md": {
    "reviewedTrees": {
      ".github/workflows/windows-signed-release.yml": "ae6d3616a176acc3c76f8801c8b00b5467b0e6da",
      "frontend/src/lib/updates.ts": "afb68a87c467af9f4e6299c2c3478a41d0762e12",
      "frontend/src-tauri/src": "873b59397b8caafaf2535be92fde61f25a7f045c",
      "frontend/src/lib/components/settings/AboutSettings.svelte": "89f4fafa004130cdac7dfb8c3c83cf6128dc67ad",
      "frontend/src-tauri/tauri.conf.json": "4db87fe6a13af1cf4b64e657867d3826743fab5c",
      "frontend/src-tauri/capabilities": "010c5218458c20ba922628f21fe030b00e655c18"
    },
    "acknowledgements": [
      {
        "date": "2026-09-16",
        "note": "Ctrl+P handed from the webview to the app, read against section 1. No sentence of this audit names it; verdict stays PASS. One path moved and the whole diff is webview_defaults.rs: VK_P leaves the list of accelerators kept with the browser, because globalShortcuts.ts now binds Ctrl+P to printing the open message, and the doc comment and two tests follow. Nothing else in frontend/src-tauri/src changed -- lib.rs, tray.rs, update_preference.rs, the three updater commands, PendingUpdate, DownloadedUpdate and beta_endpoint_override are byte-identical to a2e6d43, generate_handler! exposes the same eight commands, and capabilities/, tauri.conf.json, updates.ts, AboutSettings.svelte and windows-signed-release.yml are untouched. lib.rs also moves one assertion on WEBVIEW_DISK_CACHE_BYTES out of a test into a const assertion beside the constant, which changes no value and no argument the webview is built with, and the webview_defaults.rs doc comment gains a blank line; both are clippy cleanups with no behaviour in them. The handler this edits is attached to the webview controller in setup and is unreachable from the renderer, so section 1's IPC surface is unchanged; the key it stops keeping is a browser accelerator, which no updater path reads."
      },
      {
        "date": "2026-09-20",
        "note": "A doc comment, and nothing else. Verified the way the claim demands rather than by reading the intent: every added and removed line under this path that is neither a `///` line nor blank comes back empty. The paragraph corrected is the one the acknowledgement above introduced -- it said the app now binds Ctrl+P and prints the open message, while printCurrentView calls window.print() and is documented as printing the current view on purpose, so the next reader deciding whether a key may be denied at the WebView2 level was reasoning from behaviour the app does not have. keeps_browser_handling, is_removed_context_menu_item, attach_accelerator_keys, attach_context_menu and their tests are byte-identical, and lib.rs, tray.rs, update_preference.rs, the updater commands and generate_handler! are untouched, as are capabilities/, tauri.conf.json, updates.ts, AboutSettings.svelte and windows-signed-release.yml. No updater path reads any of it. Verdict stays PASS."
      },
      {
        "date": "2026-09-21",
        "note": "Print leaves the WebView2 context menu, read against section 1; no sentence of this audit names the context menu, and the verdict stays PASS. One path moved and the whole diff is webview_defaults.rs. Filtered to lines that are neither comment nor blank, it is exactly two things: is_removed_context_menu_item gains \"print\" in its matches!, and the test that pins that list is renamed and moves \"print\" from the kept names to the removed ones. The rest is doc comments saying why: the app now prints only the open message (globalShortcuts.ts), so a context-menu Print would print the folder list or settings page the key refuses to. keeps_browser_handling, attach_accelerator_keys, attach_context_menu and install are byte-identical, and lib.rs, tray.rs, update_preference.rs, the updater commands and generate_handler! are untouched, as are capabilities/, tauri.conf.json, updates.ts, AboutSettings.svelte and windows-signed-release.yml. The handler is attached to the webview controller in setup and is unreachable from the renderer, so section 1's IPC surface is unchanged, and no updater path reads a context-menu entry."
      },
      {
        "date": "2026-09-21",
        "note": "The startup update check's hint is hidden from the accessibility tree, read against section 1; no sentence of this audit names it, and the verdict stays PASS. One path moved and the whole diff is AboutSettings.svelte: the hint paragraph under the checkbox gains aria-hidden=\"true\" and a two-line comment saying why. The checkbox, its checked binding to updateStartupCheck, handleStartupCheckChange and setUpdateStartupCheck, the aria-describedby that still names the paragraph, the update channel select and the Check for updates button are byte-identical, as are frontend/src-tauri/src, capabilities/, tauri.conf.json, updates.ts and windows-signed-release.yml. Nothing the updater reads or writes moved: whether the startup check runs still comes from the registry value through the same two shell commands, and the change is to what a screen reader hears, not to what the control does."
      }
    ]
  },
  "docs/OAUTH_AUDIT.md": {
    "reviewedTrees": {
      "backend/src/main/java/org/voxrox/mailbackend/feature/auth": "d11da6a0f414648122ccfcaca5dc75d8d167c02e",
      "backend/src/main/java/org/voxrox/mailbackend/feature/account/service/ExternalProviderLoginService.java": "029c363ddfb5e59707b91fec6382b91be7357ff4"
    },
    "acknowledgements": [
      {
        "date": "2026-08-08",
        "note": "[Pre-structure accumulation.] Everything below was recorded in the single free-text note field the ledger carried before acknowledgements became dated records, and it therefore runs several separate reviews together in one string. It is kept verbatim rather than split, because the per-review object ids and dates it would need were never recorded. It counts as one acknowledgement; the next one starts a fresh record. The commit this id records is the change the audit's own v1.2 revision describes, so the doc and the code were written against each other rather than the doc trailing the code. Recorded here rather than by bumping `Audited commit` because the SHA would name a pre-squash branch commit that stops existing on merge — the failure mode this ledger was built for. Diff read in full: the login flow gains a fourth fail-fast guard in OAuth2LoginService (granted scopes vs. the provider's required mail scopes, rejecting before persistence), OAuth2ClaimsExtractor gains requiredMailScopes(), and the Google and Microsoft extractors state theirs. The other four Code paths entries are untouched — verified as an empty diff for SecurityConfig.java, OAuth2CompletedStateTracker.java, ExternalProviderLoginService.java and resources/static, so PKCE, the state gate and the failure-redirect hygiene are byte-identical. Checked the three things that could have moved a verdict. (1) The check only ever rejects: it compares two sets and throws, it never adds a scope to a request, so the E-row claim that scopes are hardcoded in application.properties with no in-app escalation path still holds exactly. (2) It cannot resurrect a dead account — it runs before persistence and calls markRequiresReauthIfExists, the same direction as the refresh-token guard, so the flag is still cleared only by a login that is complete. (3) No token material reaches a log: the WARN line names the missing scope constants and a masked email, and the audit entry carries the same, so the log-hygiene claim in §2 is unchanged. Verdict stays PASS. 4d3f6d6 touches this path in comments only — verified by filtering the commit diff to lines that are neither comment nor blank, which comes back empty; the whole change is javadoc on OAuth2TokenService.exchangeRefreshToken. The old text claimed the method was used during the initial OAuth2 login flow, which no call site supports: ExternalProviderLoginService persists the refresh token and leaves the exchange to the first IMAP connection, by which point the account exists and the cached getAccessToken path applies. The method was deliberately kept rather than deleted, and precisely because of this boundary — it is the only route into doRefresh(accountId = null), so removing it would also make the accountId != null branch unreachable, and that branch is what invalidates the cached token and marks the account requires_reauth after a permanent refresh rejection, which the E row and the §2 log-hygiene claim both rest on. No executable line moves, so PKCE, the state gate, the granted-scope check added in v1.2, the failure-redirect hygiene and the masked-email logging are byte-identical. Whether to wire the method back up or collapse the nullable parameter is left as its own change with its own re-verification. Verdict stays PASS. Java caller gate batch: the only change under feature/auth is three javadoc lines adding the @callerless tag to OAuth2TokenService.exchangeRefreshToken, recording in machine-readable form the decision #299 already wrote out in prose — it is kept because deleting it would make the accountId != null guard in doRefresh unreachable, and that guard is what marks an account requires_reauth. No code line changed."
      },
      {
        "date": "2026-08-26",
        "note": "Comment-only change under this path. 578526c moves the @callerless waiver on OAuth2TokenService.exchangeRefreshToken out of javadoc into a line comment, because @callerless is not a javadoc tag and Error Prone rejected it with InvalidBlockTag. Verified line by line rather than by reading the intent: every added and removed line under this path is a comment line. What the waiver protects is untouched — exchangeRefreshToken still exists, and the accountId != null guard in doRefresh it keeps reachable, the guard that marks an account requires_reauth after a permanent rejection, is byte for byte the same. No token exchange, refresh path, scope, redirect URI, PKCE step or credential storage path moved. Verdict stays PASS."
      },
      {
        "date": "2026-08-27",
        "note": "One idiom change under this path. 7ec2793 rewrites both cache lookups in OAuth2TokenService.getAccessToken from \"cached.isPresent() && cached.get().isFresh()\" to tokenCache.get(accountId).filter(CachedToken::isFresh). Optional.filter evaluates the predicate only when a value is present, exactly as the && short-circuit did, and the cache is still read once per lookup, so the freshness decision, the double-check under the refresh lock and the fall-through to doRefresh all behave as before. No token is cached, logged or transmitted differently, and no scope, redirect URI or PKCE handling is in the diff. Verdict stays PASS."
      },
      {
        "date": "2026-09-08",
        "note": "Two paths, one change, and the second path is why this note is longer than its delta. In OAuth2TokenService.doRefresh, a permanent refresh rejection now publishes AccountRequiresReauthEvent after the updateRequiresReauth / updateLastError pair it already wrote, so a listener outside this feature can close the account's now-unreachable IMAP connections; the base constructor takes the publisher and the two subclasses pass it through. ExternalProviderLoginService.markRequiresReauthIfExists is the third writer of that flag and does the same, from TransactionCallbacks.runAfterCommit because it is @Transactional - the purge behind the event waits on the per-account connection lock, which must not be waited on with the single-writer SQLite transaction open, and before the commit the flag is not durable. Nothing about either token flow moves: the same permanent-vs-transient classification decides the branch, the refresh lock, the cache invalidate, the metric and the thrown MailOperationException are unchanged, the login path still writes exactly the credentials and account fields it did, and no scope, redirect URI, PKCE parameter, client secret or token value is read, logged or transmitted differently. The event is deliberately minimal for that reason - it carries the account id alone, so no token or address reaches a listener with no business holding either. Placement inside the existing try is checked by a mutation that moves it out: the flag is what makes the account unreachable, so an event published after a failed UPDATE would ask for live connections to be closed. Three mutations in total, each red on exactly the test that claims its rule, each verified to have reached the tests. Verdict stays PASS."
      },
      {
        "date": "2026-09-17",
        "note": "A count, not a token, moves. OAuth2TokenServiceRegistry.totalCachedTokens summed getCacheStats() per service, but every service holds the one TokenCache bean, so the diagnostic summary reported each cached token once per provider; the registry now takes the TokenCache and reads its size once, and the per-service getCacheStats/CacheStats, left without a caller, are removed. No token value, refresh path, cache key or invalidation changes, so no claim of this audit moves and the verdict stays PASS."
      }
    ]
  },
  "docs/CRYPTO_STORAGE_AUDIT.md": {
    "reviewedTrees": {
      "backend/src/main/java/org/voxrox/mailbackend/core/init": "6b860914def198938000e51a328137049fffac81"
    },
    "acknowledgements": [
      {
        "date": "2026-09-11",
        "note": "Change under this path, read rather than waved through as a timing tweak. StartupTimingService swapped its reference clock: the anchor for processStartedAt and for every phase's startedAfterProcessMs moved from an Instant.now() taken in the class's own static initialiser to ManagementFactory.getRuntimeMXBean().getStartTime(), with a wall-clock constant recording how much boot had already passed when the class loaded. Section 1 is untouched by it: the diff does not enter StorageContextInitializer, reads or writes no crypto.bin, touches neither the VOXSEC1 format, the DPAPI USER-scope call, the buffer zeroing, the atomic private-permissioned write, nor the constant-time crypto.fingerprint comparison, and adds no path by which any of them could be reached earlier or skipped. The class holds no secret and opens no file. Section 2 holds: CryptoService is not in the diff, and the one place where boot timing and that section intersect - the self-test \"warmed off the boot path\" claim - is about when the self-test runs, which this change does not move; it only changes what a later dump reports about the time that had already elapsed. The new dependency is java.management, read-only and already in the sidecar's --add-modules list; it is an MXBean read, not a JNI or FFM call, so the \"pure Java FFM (no JNI)\" hygiene claim of section 1 is about a path this does not join. Section 3 residuals are unaffected - nothing about mail.db plaintext at rest or the same-user-malware exclusion turns on a clock source. The one observable effect outside the boundary is that startup-timings.json in the diagnostic dump now reports a start roughly four seconds earlier and correspondingly larger phase offsets: the same data category as before, no new field and no new value class, and the dump's privacy check is RELEASE_CHECKLIST section 7, which is flagged for a re-take on this candidate for exactly this reason."
      }
    ]
  },
  "docs/API_SURFACE_AUDIT.md": {
    "reviewedTrees": {
      "backend/src/main/java/org/voxrox/mailbackend/core/diagnostic": "2f830b8f86c784029fa29108e27385672cec65be",
      "backend/src/main/java/org/voxrox/mailbackend/feature/mail/controller": "9e1abcc70816cca08513be07583428c14aa03b27",
      "backend/src/main/java/org/voxrox/mailbackend/feature/mail/dto": "5933ad511c4a63850a3bb0ef8e59c98f849d0e4e"
    },
    "acknowledgements": [
      {
        "date": "2026-09-20",
        "note": "First note after the v1.9 re-verification of 2026-09-19, and it is what the dump omits, not what the API exposes. One path moved and the whole diff is DiagnosticDumpService: withoutHome trims a trailing separator off user.home before the prefix comparison, through a new private helper, because the comparison requires a separator after the home -- that is what keeps C:\\Users\\Lukas2 from matching the home C:\\Users\\Lukas -- and a home that already ended in one left the rest starting with a directory name, so the path came back whole with the Windows account name in it. Checked against the three things that could move a verdict here. (1) No endpoint, method, path or authentication requirement changes: the diagnostic dump is the same endpoint behind the same API key, and no controller is in this diff. (2) The response shape is identical -- the same seven JSON entries in the same ZIP, the same records, the same field names. (3) The payload can only lose what it was already meant to lose: the fix makes an existing redaction fire in a case where it silently did not, and nothing is added. Folder pseudonyms, the masked address and the rest of the bundle are untouched, and the new test drives the real createDump() with a user.home that carries a separator. Verdict stays PASS."
      },
      {
        "date": "2026-09-22",
        "note": "The message detail stops fetching the body. Two paths moved. dto: MailDetailResponse loses body and contentError, and its javadoc now says it carries everything but the body; no field is added, and the record is a response, so no request-body size limit or validation constraint is involved. controller: the OpenAPI descriptions of GET /messages/{stableId} and GET /messages/{stableId}/content now say which of the two contacts the mail server; the mappings, the path variables with their @NotBlank and @Size, and the returned types are unchanged. No endpoint, method, path, authentication rule or error mapping moves, the controller enumeration is the same, and one response loses two fields, which narrows what it discloses. Verdict stays PASS."
      },
      {
        "date": "2026-09-22",
        "note": "MailDetailResponse loses the uid component that @JsonIgnore and @Schema(hidden) kept out of the response, because the sync no longer builds this record: it has its own, FetchedMessage, in feature/mail/service. Only the javadoc and that one component moved under dto. The OpenAPI snapshot is byte-identical (OpenApiSnapshotTest passes without an update run), so no endpoint, field, constraint or disclosed value changes, and the record is still a response, never a request body. Verdict stays PASS."
      }
    ]
  },
  "docs/CONTENT_RENDERING_AUDIT.md": {
    "reviewedTrees": {
      "backend/src/main/java/org/voxrox/mailbackend/feature/mail/service/MailContentService.java": "590401e5f38d85cfe9e482486932308c3d0e022d"
    },
    "acknowledgements": [
      {
        "date": "2026-09-22",
        "note": "A comment, and nothing else: the two changed lines of MailContentService are the comment on the catch around body extraction. It said the wrapped exception lets MailFacade surface a contentError on the cached body; the message detail no longer calls this service and the field is gone, so it now says the caller gets a typed mail error instead of a 500. The sanitize call, the cid collection, the plain-text escape, the byte cap and the oversize flag are byte-identical, and the body still reaches the frame only through /content, which returns what this service produced. Verdict stays PASS."
      }
    ]
  },
  "docs/IMAP_SMTP_AUDIT.md": {
    "reviewedTrees": {
      "backend/src/main/java/org/voxrox/mailbackend/feature/mail/service": "5d2e3a4c54c8f809fd0d5704fb45e2fa6bce00eb"
    },
    "acknowledgements": [
      {
        "date": "2026-09-22",
        "note": "The fix for B1-4, recorded in the audit itself as v1.10 (sections 1 and 4d). Three files moved under feature/mail/service: ImapTransportSecurity is new and sets ssl.enable, the pinned checkserveridentity and, without implicit SSL, starttls.enable and starttls.required; ImapConnectionManager.createNewConnectedStore and MailConnectionProbe.testImap call it in place of the two properties each set by hand, and neither changes anything else. Re-read against sections 1 and 4d, which the change is about, and against the rest of section 1: the OAuth2 plaintext guard, the timeouts, partialfetch, the lanes, the retry policy and the degradation path are untouched, and so is everything after section 1. The finding moves from open to fixed; the other verdicts do not move."
      },
      {
        "date": "2026-09-22",
        "note": "The fix for B1-3, recorded in the audit itself as v1.11 (sections 2, 4c and 4g). Under feature/mail/service, BoundedImapStore and BoundedImapProtocol are new: the store makes every connection a BoundedImapProtocol, whose readResponse refuses EXISTS above 2M and VANISHED on a connection without QRESYNC, above 1M UIDs (EARLIER) or 100k (untagged), or unparseable. ImapConnectionManager.createNewConnectedStore and MailConnectionProbe.testImap register the store on the session before getStore and change nothing else; MailSyncService.buildResyncRequest returns null for a local UID range wider than the EARLIER bound; FlagSyncService.deleteVanished streams the named UIDs into batches clamped to the local UID range, reading only its two ends. Re-read against section 2's two QRESYNC bullets and section 4c, which the change is about, and against section 1's connection setup, which it touches only by the added registration: TLS, timeouts, partialfetch, lanes and retries are untouched, and so is the fetch-parse-persist path after the open. B1-3 moves from open to fixed; B1-7, found while reading for the fix, is new and open; the other verdicts do not move."
      },
      {
        "date": "2026-09-22",
        "note": "The fix for B1-7, recorded in the audit itself as v1.13 (section 4g). One file under feature/mail/service changes: BoundedImapProtocol gains getResponseBuffer(), which hands Angus a BoundedByteArray refusing to grow one response past 32 MiB, and a catch in readResponse() that turns any RuntimeException from the read into the same connection-closing IOException as a refused response. Nothing else in the class moves -- the EXISTS and VANISHED bounds of B1-3 and their parsing are untouched -- and no other file under the path changes. Re-read against section 4g, which the change is about, and against section 4c, whose residual named the literal as a separate route and now names it fixed. Section 1's connection setup, section 2's pipeline and everything after them are untouched. The finding moves from open to fixed; the other verdicts do not move."
      },
      {
        "date": "2026-09-22",
        "note": "The fix for B1-5, recorded in the audit itself as v1.14 (section 4e). Two files move under feature/mail/service, and nothing else does. ImapAppendService.fetchAndDetachMime writes the fetched draft into a ByteArrayOutputStream that refuses the first byte past 40 MiB and hands its buffer to the parser without the third copy toByteArray() was making; the detach itself -- writeTo then re-parse -- is otherwise the same call sequence. SmtpMessageService.sendDraftAsync compares the fetched copy's To, Cc and Bcc against the stored row's three fields before the transport sees the message, and on a mismatch records DRAFT_CHANGED_ON_SERVER, broadcasts the failure and returns with the draft left where it is; the send that follows an equal comparison is byte-for-byte the path that was there. Both sides of the comparison are read through HeaderAddresses.parseValidTokens, which MimeMessageBuilder already uses to build a draft's headers, so the two cannot disagree about what counts as an address and a draft this app wrote cannot be refused over tokenizing. Re-read against section 4e, which the change is about, and against what the diff could otherwise have reached: section 1's connection setup is untouched (no TLS property, timeout, lane or retry is in it), section 2's fetch-parse-persist pipeline is untouched (the fetch changed is the interactive one behind Send, not the sync's), and the bounds of B1-3 and B1-7 in BoundedImapProtocol are not in the diff. B1-5 moves from open to fixed, with the residual section 4e now states: the body and the remaining headers of a draft composed in another client stay the server's, bounded but unverified. The other verdicts do not move; B1-6 stays open."
      }
    ]
  }
}
