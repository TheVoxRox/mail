# TODO - VoxRox Mail

Aktualni pracovni seznam pro monorepo `backend/` + `frontend/`.

**Pravidlo:** jen ZIVE ukoly (`[ ]`) a rozhodnuti. Zadne implementacni deniky, audit reporty ani "HOTOVO" prozy — ty patri do commitu / `CHANGELOG.md` / docs. Tvrzeni o kodu vzdy s `file:line`, at jdou overit. **Hotovou polozku po zavreni zkrat na jeden radek** (nazev + datum + PR); plny detail patri do commitu nebo do [todo-archive.md](todo-archive.md).

Hotove reporty do 2026-06-07 jsou zamrazene v [todo-archive.md](todo-archive.md) (puvodni 748-radkovy todo). Pozdejsi hotove polozky tamtez v sekci "Presunuto z todo.md 2026-07-07".

**Po releasu:** tenhle soubor zrusit — ale nejdriv rozstehovat, ne smazat. Trvala rozhodnuti (`## Rozhodnuti`) → `docs/DECISIONS.md`; post-release backlog + rozepsane design plany (Interactive IMAP lane, Threading P2) → GitHub Issues (repo uz bude verejne); release-gate mechanika → archiv; `Dev spousteni`/`Pre-Push` → `CONTRIBUTING.md`. Az bude soubor prazdny, teprve pak ho smazat.

---

## Rozhodnuti

- Frontend a backend jsou samostatne artefakty s vlastni verzi.
- Frontend verzi drzi `frontend/package.json`, `frontend/src-tauri/tauri.conf.json` a `frontend/src/lib/version.ts`; kontroluje je `npm run check:versions`.
- Backend verzi drzi `backend/pom.xml`; runtime ji cte pres `ApplicationVersion` z Maven `build-info.properties`.
- Kompatibilitu FE/BE neresi rovnost release verzi, ale handshake pres `apiVersion` a `minClientVersion`.
- Tauri desktop bundle nese frontend i backend sidecar; po zmene backendu znovu zabalit sidecar a zkopirovat do frontendu.
- Commit messages: Conventional Commits v anglictine, scopes podle casti monorepa: `backend`, `frontend`, `tauri`, `ci`, `docs`, `repo`, `release`.
- Canonicky changelog je root `CHANGELOG.md`; modulove changelogy jen pro technicke/migracni detaily.
- **Microsoft/Outlook jede VYHRADNE pres OAuth.** IMAP+heslo neni alternativa — Microsoft vypnul basic auth pro osobni ucty (outlook/hotmail/live/msn) 16.9.2024. Kod uz odpovida (preset `flow: 'oauth'`, seed `supports_oauth2=1`). Detail viz [[project_microsoft_oauth_azure]] + archive.
- **Adresar je jeden pro celou aplikaci, ne per-ucet.** Rozhodnuto 2026-08-12, jeste pred prvnim releasem, protoze pozdeji by to byla V2 + datova migrace slucujici duplicity: `contacts` i `contact_labels` prisly o `account_id`, endpointy jsou na `/api/v1/contacts` a `/api/v1/contact-labels`. Ucet zustava jen v `/contacts/autocomplete`, kde se do adresare micha per-uctova historie korespondence (`correspondent`). Detail v [CHANGELOG.md](CHANGELOG.md).
- **`accounts.signature` zustava jeden `TEXT` sloupec bez formatu.** Rozhodnuto 2026-08-12, jeste pred prvnim releasem, protoze po nem uz je zmena migraci nad uzivatelskymi daty. Puvodni obava (Faze 2 prinese HTML compose → bude potreba `signature_html` nebo `signature_format` discriminator) padla s tim, jak vypada odesilaci cesta: editor je plain `textarea` a zustava jim (kvuli odecitaci), Markdown se interpretuje az na ceste ven a HTML alternativa je odvozenina. Podpis navic neni samostatna entita — frontend ho vklada primo do tela jako `\n\n-- \n{text}` ([signature.ts](frontend/src/lib/compose/signature.ts)), takze jde stejnou cestou jako zbytek zpravy a jeho format uz definovany je: plain text, ktery smi obsahovat Markdown. Druhy sloupec by zavedl stav, ktery nema kdo naplnit, a discriminator by rozlisoval hodnoty, ktere se na dratu chovaji stejne. „Interpretace existujicich podpisu", kvuli ktere to vypadalo nalehave, nehrozi: podpis se interpretuje pri kazdem odeslani znovu, ne jednou pri ulozeni. Invariant hlida `MarkdownBodyRendererTest` (podpisovy blok sam o sobe HTML alternativu nezapina).
- GitHub publikace je pozastavena, dokud nebude projekt kompletne lokalne odladeny.
- **`distributionSha256Sum` se do maven-wrapperu nedoplnuje.** Rozhodnuto 2026-08-17 pri revizi #279, **zduvodneni prepsano 2026-08-19** — zaver plati dal, ale dve ze tri puvodnich premis byly nepravdive. Nosny je jediny argument: pin by hlidal jeden artefakt (`apache-maven-*-bin.zip`, ktery si stahuje CodeQL autobuild), jenze ten jde po HTTPS z `repo.maven.apache.org`, tedy ze **stejneho originu jako vsechny ostatni zavislosti pomu**, ktere pinovane taky nejsou; chranil by zlomek plochy. Ozivuje se, kdyby se zaclo pinovat systematicky (cely pom), ne jen tenhle jeden soubor. Co z puvodniho zduvodneni **neplati**: (1) „neni overene, ze dependabot ten hash pri bumpu aktualizuje" — aktualizuje; `maven/lib/dependabot/maven/file_updater/wrapper_updater.rb` checksumy pred spustenim wrapperu odstrani a po nem **prepocita a vrati**, a to jen u souboru, ktere je uz mely. (2) „hodnotu by bylo nutne spocitat z vlastniho stazeni" — fakticky pravda (`.sha256` Maven Central nepublikuje, overeno 404 znovu 2026-08-19), ale je to presne to, co dependabot dela za tebe; z argumentu proti se stal argument pro proveditelnost. Zbyva jednorazovy naklad na tu prvni hodnotu. Premisa byla nepravdiva uz pri psani: Maven Wrapper podpora pristala v dependabot-core 2026-08-11 (PR #14812), sest dni pred tim rozhodnutim.
- **Vsechno se vydava pres betu.** Rozhodnuto 2026-08-28. Kazda zmena jde nejdriv jako beta a teprve po otestovani se promotuje na stable; **beta neni odkladiste rozdelane prace** — nese hotove funkce, ktere uz prosly testem vyvojaru, a znamena jen sirsi okruh uzivatelu. Duvod, proc to patri sem a ne jen do runbooku: **rusi to nalez „beta guard odmitne legitimni stable hotfix"**. V tomhle modelu nikdy nevznikne potreba vydat `0.1.1` vedle bezici bety `0.2.0-beta.1`, protoze `0.2.0` je z definice vydatelne — takze kandidat starsi nez soucasny beta manifest neni legitimni stav a cerveny `beta-channel.yml` je **spravny signal**, ne sum. Cena modelu: oprava se ke stabilnim uzivatelum dostane az po beta cyklu. Prijato pro uzavrenou betu (~45 testeru). Unikova cesta pro pripad, ze by oprava nemohla pockat, je popsana v [OPERATIONS.md](backend/OPERATIONS.md) „Model vydavani" vcetne toho, ze beta testeri tim pádem fix nedostanou, dokud nevyjde dalsi beta. Ozivuje se, kdyby se beta otevrela vetsimu publiku, kde by prodleva stable fixu prestala byt prijatelna.
- **Kontrola aktualizaci zustava jen pri startu.** Rozhodnuto 2026-08-28. [bootstrap.ts:201](frontend/src/lib/bootstrap.ts:201) je jedine volani `checkForUpdateAndPrompt`; za behu se aplikace uz neptá a druhou cestou je jen rucni kontrola v Nastaveni → O aplikaci. Puvodni argument pro periodicky check stal na tom, ze „s close-to-tray muze instalace bezet tydny bez dotazu" — pri overeni se ukazalo, ze `mail.closeAction` ma **vychozi hodnotu `quit`** ([uiLayout.ts:62](frontend/src/lib/stores/uiLayout.ts:62)), takze tray je opt-in a ve vychozim stavu je kazde spusteni kontrola. Zbyva pripad dlouho bezici relace, coz na novy timer se vsim, co k nemu patri (uklid, guard proti soubehu s otevrenym promptem i s probihajici instalaci), nestacilo. **Cena rozhodnuti byla oprava dokumentu, ne kodu:** `PRIVACY.md` i `PRIVACY.en.md` tvrdily „periodicky" a navic v sekci „Kdy komunikace probiha" psaly, ze v traye pokracuji body 1 az 3 — vcetne kontroly aktualizaci, ktera tam nebezi vubec. Opraveno na trech mistech v kazdem jazyce + resync [voxrox.org](https://voxrox.org). Ozivuje se, kdyby se menil vychozi `closeAction`, nebo kdyby se ukazalo, ze uzivatele appku bezne nechavaji bezet dny.
- **v0.1.0 = uzavrena beta:** Google zustava Testing (≤100 test-useru), bez CASA. Rozhodnuto, viz [[project_v0.1.0_closed_beta_smoke]]. **Trigger pro start CASA se nestanovuje (uzavira #170 nalez 2), rozhodnuto 2026-08-13:** beta se drzi na uvedenem stropu ~45 testeru, tedy pod polovinou capu, takze na limit nenarazi a predem stanovene cislo by hlidalo situaci, ktera nenastane. Cap je **100**, ne 50 — overeno v [Google dokumentaci](https://support.google.com/cloud/answer/15549945) 2026-08-13, protoze na tom cisle to rozhodnuti stoji. Kvota se ale **cerpa pridanim testera a odebranim se nevraci**, takze pri strindani lidi se rezerva tenci i bez rustu aktivnich — proto strop na polovine, ne na 90. Druhy limit Testing rezimu zustava znamy a akceptovany: souhlas testera i refresh token expiruji 7 dni od udeleni. Kdyby se strop bety mel zvedat nad ~70, je CASA aktualni znovu — trva tydny a neco stoji, takze se s ni nesmi zacit az pri narazu.
- **Windows installer bez Authenticode podpisu.** Rozhodnuto 2026-07-18 (uzavira #170 nalez 5): placeny code-signing certifikat se pro opensource projekt neporizuje; secret `WINDOWS_CERTIFICATE_BASE64` v repu neexistuje (overeno `gh secret list`), `windows-signed-release.yml` bali installer nepodepsany by design. Updater je kryty nezavislym Ed25519 `.sig`; prvni instalace = SmartScreen "Neznamy vydavatel" + manualni overeni pres Sigstore attestation a `.sha256` (postup v [frontend/END_USER_README.md](frontend/END_USER_README.md)). Zaznamenano jako AR-4 v [SECURITY_THREAT_MODEL.md](SECURITY_THREAT_MODEL.md) v2.4. Upgrade path, kdyby se apetit zmenil: SignPath.io OSS / Azure Trusted Signing / OV cert pri vzniku entity — workflow se podpisu chopi automaticky, jakmile secrets existuji.
- **Manifesty zavislosti se do `Code paths` auditu NEdavaji.** Rozhodnuto 2026-08-31 pri rozboru bumpu jsoup 1.23.2 (#349). Slepe misto je realne: `check:audits` porovnava objektova id souboru pod `Code paths`, `backend/pom.xml` ani `frontend/package.json` tam nejsou, takze bump knihovny muze zmenit chovani auditovane komponenty a brana zustane zelena. U #349 se to stalo doslova — jsoup je parser pod `HtmlSanitizer.java`, ktery `Code path` JE, jenze sam soubor se nezmenil. **Pridat manifest ale nejde**, a to je zmerene: `backend/pom.xml` se za poslednich 90 dni zmenil **20krat**, `frontend/package.json` **32krat**, proti stropu ledgeru **8 zaznamu / 90 dni**. Ledger by pretekl zhruba za pet tydnu a jedine vychodisko z plneho ledgeru je re-verifikace celeho auditu — brana by tedy vyrabela obrad, ne pozornost, a to je presne ta cesta k razitkovani, kvuli ktere strop vznikl. **Misto brany se posilila vrstva pod ni:** `HtmlSanitizerMalformedInputTest` tvrdi bezpecnostni invariant nad korpusem malformovaneho markupu, takze zmena chovani parseru shodi test s diffem v logu, ne branu s vyzvou k premysleni. Ozivuje se, kdyby se ukazalo, ze i jine audity stoji na chovani knihovny tak primo jako Boundary 4 (OAuth na Spring Security, IMAP/SMTP na jakarta.mail) — tam ale tvrzeni miri na nasi konfiguraci, kterou testy kryji primo. Levnejsi tvar, kdyby se to prece jen stavelo: allowlist `audit → artefakty` uvnitr stavajiciho `check-audit-freshness.mjs`, ne cely manifest; u `jsoup` to vychazi na 2 udalosti za ctvrtleti.
- **Sifrovani DB at-rest:** pro V0.1.0 vedome NEsifrujeme `mail.db`. Sloupcove sifrovani koliduje s FTS5 + ORDER BY (subject/sender/received_at), full-DB SQLCipher je odlozeno, at zustane auditovane SQLite jadro stabilni pred prvnim releasem. Reziduum = offline pristup k vypnutemu disku (ztracene/ukradene zarizeni, zkopirovana data slozka); credentials zustavaji zapecetene pres DPAPI. Mitigace = BitLocker + ACL profilu, zdokumentovano v [PRIVACY.md](PRIVACY.md), [frontend/END_USER_README.md](frontend/END_USER_README.md) a threat modelu jako AR-1 ([SECURITY_THREAT_MODEL.md](SECURITY_THREAT_MODEL.md)). Upgrade path az se zmeni apetit: SQLCipher pres drop-in `Willena/sqlite-jdbc-crypt` (Apache 2.0), DB klic zapeceteny stavajicim `SecretStore`/DPAPI.

---

## Pred prvnim commitem — manualni smoke

**Vse hotovo** (overeno 2026-06-23/24/30 v tauri:dev): DPAPI `crypto.bin`, smoke sync/OAuth/SMTP/PUT/threading, IMAP fetch robustness (Seznam, deterministicky IT), merge kontaktu, About dialog, BootErrorView Retry. Detail v [todo-archive.md](todo-archive.md). Znama pricina "Failed to launch JVM" dialogu (cfg basename) viz [[reference_sidecar_startup_debugging]].

---

## SR doposlech — audit Posty (vyzaduje uzivatele)

Deterministicke e2e kryti existuje (live-region asserty v [list-navigation.functional.e2e.ts](frontend/src/routes/mail/list-navigation.functional.e2e.ts) a spol.) — tohle je realny NVDA doposlech, ze ohlaseni skutecne zni. Opravy z SR auditu Posty (PR #118, merged 2026-07-06).

- [x] **NVDA doposlech v `tauri:dev`, rezim bez podokna cteni** — HOTOVO 2026-08-31, vsech 8 bodu odposlechnuto. Tri nalezy, dva opravene (detail v [CHANGELOG.md](CHANGELOG.md)): zkratky stinici webview delaly s kurzorem v tele zpravy obe akce naraz, takze Ctrl+Shift+G hvezdicku nastavilo, ale precetla se hledaci lista; a nativni titulek okna nenesl route, takze NVDA+T odpovidalo jen jmenem aplikace. Treti zustava otevreny nize. Split rezim pro body 1 a 2 doposlechnut 2026-09-01, viz nasledujici polozky.
- [x] **NVDA doposlech bodu 1 a 2 ve split rezimu** — HOTOVO 2026-09-01. Bod 2 (Delete) projde obema cestami, klavesou i radkovym menu: fokus pristane na sousednim radku jednou ranou (zadny odskok) a smazani se ohlasi jmenem. Bod 1 ma tri nalezy, vedene nize jako jedna polozka.
- [x] **Jedno stisknuti sipky ve split rezimu ohlasi predmet trikrat** — HOTOVO 2026-09-02, doposlechnuto. Tri priciny nize opraveny a doposlech potvrdil, ze jsou pryc (jeden fokus na sipku, announcer neslysitelny) — **a vydal ctvrtou, o ktere nikdo nevedel**, vedenou samostatne nize. Zmereno v bezici appce pres CDP (zaznam fokusu + zapisu do zivych oblasti), ne odhadnuto sluchem: fokus na bunku (1.), `#svelte-announcer` asertivne titulek route (2.), fokus spadne na `<body>` a o 1 ms pozdeji se vrati na tutez bunku (3.). Tri nezavisle priciny, kazda opravitelna zvlast:
  1. ~~`selectAndFocus` obnovuje fokus v `finally` po `handleSelect`, protoze ho navigace mezitim resetuje na `<body>`~~ — **OPRAVENO 2026-09-01**: `handleSelect` navaduje s `keepFocus`, takze fokus z bunky nikdo nesundava a neni co obchazet. S obchazkou padl i token na zastarale refokusy (`createLatestSelection` uz plochy seznam nepouziva, drzi ho jen `ConversationList`). Kryti: "sipka ve split rezimu zaostri radek jen jednou" v [list-navigation.functional.e2e.ts](frontend/src/routes/mail/list-navigation.functional.e2e.ts) — pocita fokusove udalosti, protoze sousedni test poll-uje koncovy stav a odskok konci tam, kde zacal. Overeno reverzi: na starem kodu je log `["msg-02", "BODY", "msg-02"]`.
  2. ~~`#svelte-announcer`, vestaveny oznamovac SvelteKitu (`aria-live=assertive`, tedy prerusuje), hlasi titulek pri kazde navigaci — a ve split rezimu je navigace **kazda sipka**. Tyka se i mazani: po smazani zazni asertivne "Posta - Dorucene" **pred** politem hlasenim "Zprava smazana: ..."~~ — **OPRAVENO 2026-09-02**: umlceno pres `display: none` v [app.css](frontend/src/app.css), cimz element vypadl z accessibility stromu. Duvod neni jen duplicita — `assertive` vyprazdnuje frontu reci, takze se nezaradil za polita hlaseni aplikace, ale pred ne. Redundance, kterou oznamovac jinde kupuje, sem nesedi: existuje pro dvojice, ktere neohlasi presun fokusu (NVDA s Firefoxem, VoiceOver se Safari), a tady bezi jeden engine, WebView2; "kde jsem" na vyzadani drzi nativni titulek okna (NVDA+T). Atributem to nejde, element je generovany kod a SvelteKit nema vypinac. **Zmereno pri psani testu:** po navigaci na stranku zkratek je `document.title` spravne "Posta - Klavesove zkratky", ale v oznamovaci stoji "VoxRox Mail" — SvelteKit vzorkuje titulek o tick driv, nez dorazi `<svelte:head>` nove route, takze ta asertivni vsuvka jmenovala stranku, ze ktere uzivatel prave odesel. Nativniho titulku okna se to netyka, ten se zrcadli z `document.title` pres observer. Kryti: "vestaveny oznamovac route je umlceny" v [a11y.e2e.ts](frontend/src/routes/a11y.e2e.ts) — tvrdi, ze SvelteKit do oblasti porad zapisuje i ze ji neni slyset. Overeno reverzi: na starem kodu pada na `toBeHidden()`.
  3. ~~Baner blokovanych vzdalenych obrazku v [MessageContent.svelte](frontend/src/lib/components/message-detail/MessageContent.svelte) je `aria-live=polite`, takze se pri kazdem prenacteni podokna precte cely vcetne obou tlacitek~~ — **OPRAVENO 2026-09-02**: zivou oblasti byt prestal. Zduvodneni v komentari u nej ("nekrade fokus automaticky zaostrenemu telu") plati pro otevreni Enterem, ne pro prochazeni seznamu. Ohlaseni nic nenahrazuje, jen dosazitelnost: region si nechava jmeno pro klavesu D a obe tlacitka zustavaji v poradi fokusu mezi radkem a telem. Kryti: "baner blokovanych obrazku se necte sam" v [remote-images.functional.e2e.ts](frontend/src/routes/mail/remote-images.functional.e2e.ts) — hleda zivou oblast i mezi predky, aby ji neslo obnovit o patro vys. Overeno reverzi: na starem kodu pada s `div[aria-live=polite]`.

  **Doposlech 2026-09-02 potvrdil, ze vsechny tri jsou pryc** — a odhalil ctvrtou, strukturalni: viz nasledujici polozka.

- [x] **Prepnuti slozky nerekne, ve ktere slozce jsi** — HOTOVO 2026-09-02. Otevreny konec po umlceni oznamovace route, doposlechnuty a uzavreny. Zmereno: po kliknuti na slozku se fokus presune na `<main>`, jenze ten se jmenoval podle **prostredi** ("Posta"), takze odecitac rekl prostredi a pak zacal cist obsah — ve split rezimu prazdny placeholder podokna cteni ("Vyberte zpravu ze seznamu…"). Nazev slozky nezaznel nikde; slo ho ziskat jen na vyzadani pres NVDA+T, ktery titulek okna nese spravne. Politou hlasku "Strana 1 z 1, 25 zprav" appka posila taky, ale az za 2,3 s a slozku nejmenuje. `<main>` se proto jmenuje podle route: bere `document.title` z tehoz observeru, ktery uz zrcadli nativni titulek okna, takze v layoutu nevznika druha kopie logiky route (tentyz duvod, jaky si u titulku vyminuje komentar vedle). Kryti: "hlavni oblast se po prepnuti slozky jmenuje podle slozky" v [list-navigation.functional.e2e.ts](frontend/src/routes/mail/list-navigation.functional.e2e.ts) — tvrdi to nad **dvema** slozkami, protoze jmeno, ktere je spravne jen jednou, by proslo i jako konstanta. Overeno reverzi: `Expected: "Posta – Dorucene" / Received: "Posta"`.
      **Podmineno uzivatelem:** ohlaseni ted zni "Posta – Dorucene, hlavni oblast" a uzivatel si ho **nechava zatim** s tim, ze bude sledovat, jestli ho casem neotravuje. Kdyby ano, kratsi varianta je jen nazev slozky bez prefixu "Posta – " (jednoradkova zmena v `mainLandmarkLabel`).

- [x] **Tri stranky nastaveni si titulek predznamenavaly jmenem aplikace** — HOTOVO 2026-09-02: Vzhled, Jazyk a O aplikaci vedou produktovym jmenem jako zbytek appky (uroven "Nastaveni" podle skupiny v podoknu, jak uz to delaly Ucty a Zkratky), takze odstrihavani prefixu v [+layout.svelte](frontend/src/routes/+layout.svelte) ztratilo duvod a je pryc. Detail v [CHANGELOG.md](CHANGELOG.md).

- [x] **Bunka predmetu se cetla dvakrat, protoze fokus nesl odkaz uvnitr ni** — HOTOVO 2026-09-02. Ctvrta, do te doby nezname pricina trojiho ohlaseni; jina svym druhem nez tri vyse, ktere byly casove (udalost navic). Tahle je **strukturalni**, takze ji pocitani fokusovych udalosti najit nemohlo: bunka predmetu je jedina v gridu, kde je text i zaostritelny prvek zaroven **a shodny**, takze odecitac precetl bunku, do ktere vstoupil, a pak jeste odkaz v ni ("test, radek 3, predmet, sloupec 3, test, odkaz"). Ostatnich pet bunek ma bud text bez prvku, nebo prvek bez textu. Roving tabindex proto sedi na `div[role="gridcell"]` a odkaz ma `tabindex="-1"`, cimz se sloupec predmetu srovnal s konvenci, kterou zbylych pet uz drzelo. **Komentar u [MailSubjectCell.svelte](frontend/src/lib/components/mail-list/MailSubjectCell.svelte) tvrdil opak** — ze odkaz musi nest roving tabindex, jinak ho browse mode neaktivuje; doposlech ukazal, ze s `tabindex="-1"` si odkaz nechava `href` i roli, virtualni kurzor ho najde a Enter zpravu otevre. Komentar prepsan. Kryti: "fokus nese bunka predmetu, ne odkaz uvnitr ni" v [list-navigation.functional.e2e.ts](frontend/src/routes/mail/list-navigation.functional.e2e.ts) — tvrdi to pres `[data-cell-target]`, tedy pres to, kam grid fokus opravdu vodi, protoze selektor mirici rovnou na bunku pada hlaskou "element nenalezen", ktera o pricine nerekne nic. Overeno reverzi: `Expected: "gridcell" / Received: ""`.
- [x] **`ConversationList` mel tutez vadu** — ANO, potvrzeno poslechem 2026-09-02: predmet se v seskupenem rezimu cetl dvakrat. Opraveno stejne jako u plocheho seznamu (tabindex na `div[role="gridcell"]`, odkaz `tabindex="-1"`), kryto testem „fokus nese bunka predmetu konverzace, ne odkaz uvnitr ni", overeno reverzi. Aktivace doposlechnuta tamtez: Enter na radku konverzace zpravu otevre a predmet zazni jen jednou. Detail v [CHANGELOG.md](CHANGELOG.md).

- [ ] **Otevreni radkoveho menu vyvola ctyri fokusy misto dvou** — kontejner a prvni polozka, a obojí znovu o 3 ms pozdeji. Zmereno pri doposlechu bodu 2, ale **dopad neoveren**: neni potvrzeno, ze to odecitac cte dvakrat, a uzivatel si toho pri poslechu nevsiml. Overit sluchem driv, nez se to zacne opravovat.
- [x] Sync neohlasi dokonceni — HOTOVO 2026-09-01: backend posila `sync_cycle_completed` na konci **rucniho** pruchodu vcetne nuly (planovany zustava tichy), zamek nese `reportOnFinish` a zamitnuty rucni pozadavek eskaluje bezici pruchod, `syncing` v sidebaru drzi novy store a ceka na tu udalost. Detail v [CHANGELOG.md](CHANGELOG.md). **Zbyva doposlech nize.**
- [x] **Tlacitko Synchronizovat zmizi z klavesnice na celou dobu syncu** — HOTOVO 2026-09-01: `disabled` nahrazeno `aria-disabled` (klik uz odmita `handleSync` sam), takze tlacitko zustava zaostrene i v poradi fokusu a `aria-busy` na nem ma komu neco rict. Kryti: "tlacitko Synchronizovat zustane zaostrene po celou dobu pruchodu" v [sync.functional.e2e.ts](frontend/src/routes/sync.functional.e2e.ts); overeno reverzi, na starem kodu pada na `Expected: focused, Received: inactive`. Puvodni nalez: Nalez z doposlechu 2026-09-01, potvrzeny mereni i sluchem. `disabled={syncing || !$activeAccount}` v [MailSidebar.svelte:148](frontend/src/lib/components/sidebar/MailSidebar.svelte:148): zakazani **zaostreneho** prvku ho vyradi z poradi fokusu a prohlizec fokus tise zahodi na `<body>` — bez fokusove udalosti, takze v zaznamu je videt jen vysledek. Uzivatel tedy zmackne Synchronizovat a je nikde; Shift+Tab uz tlacitko nenajde, objevi se az po dokonceni. Trva to cely pruchod, tedy az po tri minuty stropu. `aria-busy` na tlacitku je, ale na prvku, na ktery se neda dostat. Projekt tenhle vzorec jinde resi spravne — tlacitko Napsat u kontaktu bez adresy je `aria-disabled` prave proto, aby v poradi fokusu nevznikla dira; tady se to nedodrzelo.
- [x] **NVDA doposlech ohlaseni konce syncu** — HOTOVO 2026-09-01, vsechny tri vetve odposlechnuty a **vsechny funguji**. Prvni beh se skutecne prebalenym sidecarem: do te doby `sync_cycle_completed` v bezici appce neexistoval, takze doposlech driv nemel co slyset. Pruchod bez posty rekl "Synchronizace dokoncena, zadne nove zpravy."; pruchod s postou rekl zahajeni, slozkovy toast s poctem a pak **prosty** souhrn bez poctu, pricemz obava z revize, ze to bude znit jako dve hlaseni o tomtez, se nepotvrdila (mezi toastem a souhrnem uplynulo 11 s). Pad SSE streamu, vyvolany zabitim sidecaru uprostred pruchodu, uvolnil cekani po 7,5 s a rekl "Synchronizace pokracuje na pozadi." — tedy hned, ne az po tristropovem cekani. Overeno i to, ze dve hlaseni tesne za sebou **nejsou** vada: [LiveAnnouncer.svelte](frontend/src/lib/components/LiveAnnouncer.svelte) dava kazdemu vlastni `<p>` a oblast ma `aria-atomic="false"`.
- [ ] **NVDA doposlech seskupeneho rezimu** (threading Phase 2, `mail.messageGrouping = grouped`) — odposlechnute a bez nalezu: radek konverzace a jeho aktivace (polozka vyse) a **rozbaleni/sbaleni vlakna** (2026-09-02, funguje). **Hromadne akce odposlechnute tamtez**: pocty sedi (mazani zasahne jen aktualni slozku, jak tvrdi test), ale ohlaseni cetlo „selhalo: 0" po kazde uspesne akci — opraveno, detail v [CHANGELOG.md](CHANGELOG.md). Zbyva uz jen badge cizi slozky. **Pozor, priklad tady byl spatne:** badge nikdy nerekne „Ve slozce Odeslane" — Odeslane jsou spolu s Kosem, Spamem a Rozepsanymi vyloucene z agregace pres slozky (`CONVERSATION_EXCLUDED_ROLES` v [MailFacade.java:40](backend/src/main/java/org/voxrox/mailbackend/feature/mail/service/MailFacade.java)). Realny pripad je „Ve slozce Dorucene", tedy kopie z Dorucenych uvnitr archivovaneho vlakna. Pozor na to, co e2e nezachyti: ze ohlaseni odpovida skutecnosti a ze ovladaci prvek jde dosahnout jinak nez mysi — oba dosavadni nalezy v tomhle rezimu byly tohohle druhu (viz [[feedback-sr-intercepts-single-key-shortcuts]]).
- [x] Clenske radky vlakna uz neopakuji predmet — HOTOVO 2026-08-18: predmet nese jen radek konverzace, clen nese s kym a kdy. Jmeno zustalo ve sloupci Odesilatel a bunka predmetu se u clenu vyprazdnila (rozhodnuto s uzivatelem), takze hlavicky sloupcu dal sedi a clensky radek je jednoradkovy. Kotva cteni se u clenu posunula na COL_SENDER — dotklo se to fokusu po rozbaleni, ArrowLeft na rodice i obnovy fokusu po radkove akci. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] Ploche seznamy zprav maji sdilene sloupce — HOTOVO 2026-08-18: kontejner drzi tracky, radky jsou `grid-cols-subgrid`, stejne jako `ConversationList`. Oproti zadani zahrnut i [SearchResultsGrid.svelte](frontend/src/lib/components/SearchResultsGrid.svelte) — sdili bunku `MessageFlags`, takze mel tutez vadu. Vsechny tri zaznamenane pasti potvrzeny (`content-start`, zadny padding na radku, pocet tracku != `aria-colcount`). Kryti: [list-columns.functional.e2e.ts](frontend/src/routes/mail/list-columns.functional.e2e.ts). Detail v [CHANGELOG.md](CHANGELOG.md).
- [ ] **NVDA doposlech indikatoru selhavajiciho syncu** (#226, merged 2026-08-07) — nova plocha, kterou predchozi dva body nepokryvaji: sticky toast pri `sync_failed` a trvale tlacitko v sidebaru vedle Synchronizovat ([MailSidebar.svelte](frontend/src/lib/components/sidebar/MailSidebar.svelte)), vedouci do Nastaveni → Ucty. Overit, ze toast ohlasi duvod z lokalizovaneho `lastError` a ze se na tlacitko da dostat klavesnici; e2e uz dokazuje, ze je fokusovatelne, ne ze ohlaseni zni srozumitelne.

---

## v0.1.0 smoke — bugy Faze B

**Vse uzavreno** (2026-06-25/30). 8 bugu opraveno: FE update-dialog (#65), OAuth poll reconcile (#66), "duch" zpravy 404 A+B, SSE 30min ERROR (C), soubezny sync UNIQUE (F), HHH90003004 paginace (#70), duplicitni OAuth callback (#71/E), transient sync retry (#78/D), §6 sidecar zombie watchdog (#89/#90). Detail v [todo-archive.md](todo-archive.md); reziduum = pasivni log-watch v [backend/RELEASE_CHECKLIST.md](backend/RELEASE_CHECKLIST.md) §8.

---

## First Release Gate

Kanonicky seznam _kroku_ je [backend/RELEASE_CHECKLIST.md](backend/RELEASE_CHECKLIST.md): fresh install (§3/§3a), account + OAuth flows (§4), mail workflows (§5), sidecar lifecycle + recovery (§6), diagnostics (§7), long run (§8). Tady drzime jen release-mechaniku, ktera v checklistu NENI, a rozhodovaci body.

Rozhodnuti, ktera zamykala schema, jsou hotova — adresar i format podpisu, obe v
sekci Rozhodnuti. Zbytek DB analyzy (katalog provideru mimo migraci, drift gate,
redundantni indexy, `NOT NULL`) taky, detail v [CHANGELOG.md](CHANGELOG.md).

- [ ] Projit cely `backend/RELEASE_CHECKLIST.md` pro konkretni kandidat (vc. §6 recovery a §8 long run).
- [ ] End-to-end release dry run z cisteho checkoutu: tag / draft release, Windows signed workflow, upload artefaktu, overeni instalatoru, `latest.json`, podpisu, rucni instalace na cistem profilu.
- [ ] Release proces: verze, tag, changelog, known issues, hashe artefaktu, draft vs public, approval.
- [ ] **Google OAuth — Publish + CASA pro verejnost.** Projekt `VoxRox` je zatim **Testing** (External). Restricted scope `https://mail.google.com/` ([application.properties:90](backend/src/main/resources/application.properties)) vyzaduje pro public **Publish + CASA verifikaci** (Tier 2, per-projekt, placene/rocni). Pro v0.1.0 se NEresi — closed beta zustava Testing bez CASA (viz Rozhodnuti + [[project_v0.1.0_closed_beta_smoke]]). Produkcni creds + consent screen uz hotove (archive). Vetsi blok nez creds → soucast produkcni OAuth readiness.
- [ ] Privacy/legal balicek — CZ+EN draft hotov ([PRIVACY.md](PRIVACY.md) / [PRIVACY.en.md](PRIVACY.en.md)); support email, security disclosure kontakt ([SECURITY.md](SECURITY.md)) i Tauri updater URL doplneny. Zbyva uz jen **pravni review** (GDPR "spravce dat" pro org. nasazeni).
- [ ] Third-party license audit — inventare + bundled NOTICE.txt + SBOM hotove; pred kazdym release regenerovat (`npm run regen:licenses:all`). Overeno 2026-06-30: regen = 0 license drift (631 komponent). Dalsi beh az pri samotnem release (tam se NOTICE.txt commituje) — do te doby NEopakovat.

---

## Microsoft OAuth — zbyva pro produkci

Rozhodnuto: OAuth-only (viz Rozhodnuti). PKCE explicitne zapnut v [SecurityConfig](backend/src/main/java/org/voxrox/mailbackend/core/config/SecurityConfig.java). Detail kontextu [[project_microsoft_oauth_azure]] + archive. Public client migrace (RFC 8252), smoke `@outlook` login + SMTP a MS 465→587/STARTTLS fix (PR #58) jsou **hotove** — detail v [todo-archive.md](todo-archive.md).

- [ ] **Verified publisher (Partner One ID, drive MPN) — ODLOZENO (rozhodnuto 2026-06-20: zatim bez pravnicke osoby).** NENI blocker prvniho release pro cilove publikum: osobni MS ucty (`@outlook`/`@hotmail`/`@live`) i vlastni `@voxrox.org` tenant dostanou consent i bez nej — jen uvidi "unverified" notici. Tvrdy blok (`AADSTS700016`) plati JEN pro cizi organizacni Entra tenanty (B2B), a to publikum je odlozeno, dokud nevznikne pravnicka osoba. Prereq "app pod Entra work/school uctem" uz splnen (info@voxrox.org). **Az/pokud vznikne entita**, postup (overeno 2026-06-20 z MS docs):
  1. Mit overeny **Microsoft AI Cloud Partner Program** ucet = **Partner Global Account** s dokoncenou business verifikaci (tax ID / D-U-N-S) → da **Partner One ID** (globalni). Registrace zdarma, vetting trva dny = ten pomaly krok.
  2. DNS-overit **`voxrox.org`** v tenantu Default Directory a nastavit jako publisher domain appky (musi sedet s domenou e-mailu pouziteho pri verifikaci Partner uctu).
  3. Role na `info@voxrox.org`: Entra `Application Administrator`/`Cloud Application Administrator` + Partner Center `CPP Partner Admin`/`Account Admin`; prihlasit pres MFA.
  4. portal.azure.com → Default Directory → App registration `VoxRox Mail` → Branding & properties → pole **Partner One ID** → **Verify and save** (~5 min, jakmile 1-3 sedi). Overit: consent obrazovka ukaze "Verified", AADSTS700016 zmizi.
  - **Pozor:** verified publisher NEobejde enterprise tenanty s vypnutym user-consent — ty stejne vyzaduji RUCNI admin-consent jejich IT. MSA + vlastni @voxrox.org projdou i bez toho.

---

## Release & Update (Tauri updater)

Ed25519 signing key + offline zaloha klice — **hotove** (2026-06-16/20), detail v [todo-archive.md](todo-archive.md).

- [ ] Overit podepsane release artefakty na updater URL: `voxrox-mail-<version>-windows-x64-setup.exe` + `.sig` + `latest.json` (= RELEASE_CHECKLIST §3a, tady jako updater-specificky gate).
- [ ] Otestovat prvni update pres Tauri updater + smoke vN-1 → vN (bez ztraty dat, bez GUI regrese). **Nejdulezitejsi bod release gate — updater se neda opravit pres updater, takze rozbity updater ve verzi, kterou uz lidi maji, je neopravitelny.** **Rozhodnuto 2026-06-30:** smoke se NEdela predem — repo `TheVoxRox/mail` je public a publikace je pozastavena, takze prod-endpoint test by vyzadoval verejne publikovany 0.1.1. Provede se jako posledni krok tesne pred prvnim verejnym shipem 0.1.0. Fresh install (§3/§3a), §4 Gmail OAuth + restart token refresh (DPAPI), §5 mail workflows i §6 watchdog (#90) uz overeny na signed buildu 0.1.0 (drzeny jako draft `v0.1.0` s cerstvym buildem z main vc. #94). **Tenhle smoke je zaroven jediny realny dukaz shody podpisoveho paru:** kdyz embedded `pubkey` (base `tauri.conf.json` / CI `TAURI_UPDATER_PUBKEY`) neodpovida `TAURI_SIGNING_PRIVATE_KEY`, updater odmitne `.sig` se signature error a instalace neprobehne — neopravitelne po shipnuti, proto ho nejde preskocit.

### Nalezy z revize updateru (2026-08-27)

Staticka revize cele aktualizacni cesty (Rust prikazy, [updates.ts](frontend/src/lib/updates.ts), dialogy, config-prep skripty, oba workflow, guard, runbook). Duveryhodnostni retezec je v poradku — podpis se overuje nativne, webview nema zadnou `updater:` capability, kanal je enum misto URL a version pin na pending slotu drzi. Nalezy jsou v provozni a UX polovine.

- [x] **CI gate na shodu podpisoveho paru** — HOTOVO 2026-08-27: [verify-updater-signature.mjs](frontend/scripts/verify-updater-signature.mjs) overuje instalator proti `pubkey` z vygenerovaneho release configu, krok v [windows-signed-release.yml](.github/workflows/windows-signed-release.yml) shodi build pred publikaci. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] **Instalator nekontroluje bezici sidecar** — HOTOVO 2026-08-27: zavod nahrazen poradim. Stahovani a instalace jsou dva prikazy (`download_pending_update` / `install_pending_update`) a mezi nimi [updates.ts](frontend/src/lib/updates.ts) zastavi sidecar, takze install adresar je volny drive, nez instalator zacne. Potvrzeno, ze `install_inner` na Windows konci `std::process::exit(0)`, tedy `beforeunload` se nespousti. Detail v [CHANGELOG.md](CHANGELOG.md).
- [ ] **Update smoke musi doslova overit prepis `runtime/`.** Zbytek po predchozim bodu: poradi je opraveno v nasem kode, ale ze instalator opravdu prepise `runtime/**` a `app/**` bez „file in use", nedokaze zadny test — jen realny vN-1 → vN update. `CheckIfAppIsRunning` v [nsis-installer.nsi:640](frontend/src-tauri/windows/nsis-installer.nsi:640) dal hlida jen `${MAINBINARYNAME}.exe`, takze pro cesty mimo nas update flow (rucni reinstalace pres stazeny installer, kdy appka bezi) sidecar nekryje nic. Pokud smoke selze, dalsi krok je pridat kontrolu na sidecar launcher primo do `.nsi`.
- [x] **Instalace nema progress ani informaci, ze se aplikace ukonci** — HOTOVO 2026-08-28: `download_pending_update` emituje `update://download-progress` (skrceno na cela procenta), [updates.ts](frontend/src/lib/updates.ts) drzi fazi `downloading` → `stoppingBackend` → `installing` a [UpdatePromptDialog.svelte](frontend/src/lib/components/UpdatePromptDialog.svelte) vykresluje progressbar + nazev faze. Faze a hrube kroky 25/50/75 % se hlasi pres celoaplikacni polite region, ne zivym barem. Popis rika, ze se aplikace ukonci. Audit v1.7, threat model v2.16. **Zbyva NVDA doposlech** — e2e dokazuje, ze ohlaseni do live region dorazi, ne ze zni srozumitelne; patri k bodum v sekci „SR doposlech". Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] **„Pozdeji" a Esc znamenaji „tuhle verzi uz nikdy"** — HOTOVO 2026-08-28, rozhodnuto s uzivatelem (oddelit skip od zavreni): `postponePromptedUpdate` uz jen zavira dialog, verzi zapisuje jedine nova `skipPromptedUpdateVersion` za vlastnim tlacitkem „Preskocit tuto verzi". Pri overovani se zapis v puvodni polozce ukazal jako sirsi, nez ve skutecnosti byl: `wasDismissed` se cte **jen** v `checkForUpdateAndPrompt`, takze preskoceni umlci startovni prompt, ne update — rucni kontrola v Nastaveni verzi nabidne dal a klic drzi jedinou verzi. Obojí pribito testem. Varianta „nepersistovat vubec" zamitnuta: vyrabi modal pri kazdem studenem startu, cemuz se kod u selhani kontroly vedome vyhyba kvuli odecitaci. Audit se nemeni (§5 tvrzeni o best-effort dismissal dal plati), jen ack v [audit-freshness.json](docs/audit-freshness.json). Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] **Kontrola bezi jen pri startu** — UZAVRENO 2026-08-28 rozhodnutim uzivatele: **periodicky check se nedela**, misto toho se narovnaly dokumenty. Viz Rozhodnuti.
- [x] **Selhani updatu se ohlasi dvakrat a ve dvou modalech nad sebou** — HOTOVO 2026-08-28: toast zrusen (modal je hlasitejsi i uzitecnejsi — bere fokus, nese chybovou zpravu i fallback link a musi se zavrit) a prompt se pri selhani zavira misto navratu do `available`. Update se neztraci, verze nebyla oznacena jako dismissed. E2e nove tvrdi i to, ze fokus je uvnitr failure dialogu. Audit se nemeni (zadne jeho tvrzeni tohle nebere), jen ack v [audit-freshness.json](docs/audit-freshness.json). Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] **Beta guard odmitne legitimni stable hotfix** — UZAVRENO 2026-08-28: **nalez neplati**, protoze premisa „legitimni" v modelu vydavani neexistuje. Viz Rozhodnuti (vse pres betu). Guard ma pravdu, ze je cerveny; doplnen runbook v [OPERATIONS.md](backend/OPERATIONS.md) „Model vydavani". Bez zmeny kodu.
- [x] **Script injection v obou workflow** — HOTOVO 2026-08-28 (nalez **U-1** v [UPDATER_AUDIT.md](docs/UPDATER_AUDIT.md) §6, Low, opraveny). Kazda hodnota zvenci jde pres step-level `env:` a cte se jako promenna; prevedeny i oba booleovske vstupy, takze v zadnem `run:` bloku obou souboru nezustal zadny vyraz. Audit v1.6, threat model v2.15. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] **Vyber artefaktu pro `latest.json` je skore, ne tvrzeni** — HOTOVO 2026-08-28: shoda verze je nove podminka aplikovana pred razenim, jako cely token (`0.1.0` nesedi uvnitr `0.1.01`); skore uz jen rozhoduje remizy. Chybejici podepsany artefakt se hlasi zvlast od neshody verze. Skript nemel zadne testy, nova sada bezi proti odkladacimu `bundle/` adresari (8 testu, odstraneni podminky shodi 4). Zaznamenano i to, ze brana na shodu podpisoveho paru tenhle pripad chytit nemuze — overuje artefakt, ktery manifest jmenuje. Audit v1.8, threat model v2.17. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] **Drobnosti updateru** — HOTOVO 2026-08-28 (jedna davka):
  - ~~guard v `installPromptedUpdate` propousti stav `installing`~~ — HOTOVO 2026-08-27 pri oprave poradi instalace.
  - ~~`showMockUpdateFailureForTests` je vystaveny do `__MAIL_E2E__`, ale zadny test ho nevola~~ — **smazan**; failure cesta dal jede pres `showMockUpdateForTests(..., { failInstall: true })`.
  - ~~[build.rs](frontend/src-tauri/build.rs) neemituje `cargo:rerun-if-env-changed=TAURI_UPDATER_BETA_ENDPOINT`~~ — **doplneno**; `option_env!` cte hodnotu pri prekladu a cargo ji sam nesleduje.
  - ~~skript pro updater-only config nevolal zadny workflow ani skript~~ — **reziduum, smazano** (skript, npm entry, `.gitignore` radek i zminka ve [WINDOWS_SIGNING.md](frontend/docs/WINDOWS_SIGNING.md)). Fork z puvodniho zapisu rozhodnut mereni: bez Authenticode certifikatu, coz je stav tohohle repa (AR-4), vyrabel **bajtove identicky** soubor jako [prepare-tauri-windows-release-config.mjs](frontend/scripts/prepare-tauri-windows-release-config.mjs) — overeno spustenim obou a diffem.
  - ~~Informativni poznamka z [UPDATER_AUDIT.md](docs/UPDATER_AUDIT.md) §6 (CI lint, ze `dangerousInsecureTransportProtocol` neni `true`) je otevrena od verze 1.0~~ — **zavrena**; asercí ve [verify-updater-signature.mjs](frontend/scripts/verify-updater-signature.mjs), ktery ten config uz stejne cte. Audit v1.9. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] **Revize [UPDATER_AUDIT.md](docs/UPDATER_AUDIT.md) po opravach** — HOTOVO 2026-08-28: audit v1.5 preoveren proti `c6744a1` (route 1), zaznam v [audit-freshness.json](docs/audit-freshness.json) smazan, oba stropy vynulovany. Verdikt PASS drzi. Vsechna tri mista opravena (§1 tri prikazy + dvojity version pin, §2 nova brana na shodu klicu spolu se zaznamem, co „fail-closed" do 2026-08-27 nekrylo, injection zapsana jako nalez **U-1** v §6), `Code paths` rozsireny. Threat model v2.14, index verdiktu radek B6. Detail v [CHANGELOG.md](CHANGELOG.md).

---

## Bezpecnost (release-ops)

**Hotovo** (detail v [todo-archive.md](todo-archive.md)): produkcni Google OAuth creds + consent screen (2026-06-23), git-history secret scan (gitleaks, 0 leaks), vuln-scan.yml (SBOM + OWASP DC + Trivy + cargo/npm audit) + NVD API key, cargo audit gate, Tauri capability/CSP/IPC audit, zostrena globalni CSP, log hygiene audit, CodeQL log-injection (72 alertu, PR #24), Mail TLS hardening (PR #25/#26), recovery runbook OPERATIONS.md.

- jackson-databind CVE-2026-54515 — override `jackson-2-bom 2.21.5` aplikovan 2026-07-10, OSV mirror dotazen a scan ciste (overeno 2026-08-06).
- [ ] **Backend overrides v [backend/pom.xml](backend/pom.xml) — cekaji na Spring Boot.** Docasne bezpecnostni piny; odstranit, az je Boot dozene. Kontrolovat pri kazdem Boot bumpu (4.1.1 = posledni k 2026-08-26; vysla 2026-08-20 a dohnala tri ze ctyr):
  - ~~`<jackson-2-bom.version>2.21.5</jackson-2-bom.version>`~~ (CVE-2026-54515) — **odstraneno 2026-08-26**, Boot 4.1.1 managuje 2.21.5.
  - ~~`<jackson-bom.version>3.1.5</jackson-bom.version>`~~ (CVE-2026-59889, `@JsonView` obchazen u `@JsonUnwrapped` pri deserializaci, CVSS 6.5, advisory 2026-07-21) — **odstraneno 2026-08-26**, Boot 4.1.1 managuje 3.1.5. Slo o primarni JSON stack (`spring-boot-starter-web` → `starter-jackson` → `tools.jackson.core:jackson-databind`), ne bridge bom.
  - `<tomcat.version>11.0.25</tomcat.version>` (CVE-2026-66299) — **jediny zbyvajici.** Odstranit az managed `tomcat.version` >= 11.0.25. Boot 4.1.1 spravuje 11.0.24 (overeno 2026-08-26).
  - ~~`<log4j2.version>2.25.5</log4j2.version>`~~ (CVE-2026-49844) — **odstraneno 2026-08-26**, Boot 4.1.1 managuje 2.25.5.
- [x] Tomcat CVE-2026-66299 — HOTOVO 2026-08-19: fix verze 11.0.25 vysla 2026-08-18 (devet dni po tom, co polozka vznikla) a pin je na ni bumpnuty. Nebylo to u nas exploitovatelne (zranitelna je jen `examples` webapp, kterou embedded Tomcat nenese), slo o vycisteni code-scanning alertu #125. Detail v [CHANGELOG.md](CHANGELOG.md).
- swagger-ui webjar 5.32.11 → 5.32.13 (DOMPurify 3.4.13) — HOTOVO 2026-08-17, zavira alerty #130/#131. Pin je nove nutne parovat s `springdoc.swagger-ui.version`, jinak Swagger UI v devu vraci 404; removal condition drzi komentar u zavislosti v [backend/pom.xml](backend/pom.xml), detail v [CHANGELOG.md](CHANGELOG.md).
- [ ] **Frontend overrides v [frontend/package.json](frontend/package.json) — chybely removal conditions, doplneno 2026-08-07.** Kazdy je pin bez data expirace; historie ukazuje, ze pin sam casem spadne do zranitelneho rozsahu (`js-yaml` 4.3.0, `postcss` 8.5.12, `brace-expansion` 2.1.2/5.0.7 — vsechny tri se to uz staly). Stav k 2026-08-07 (`npm audit` cisty na vsech severitach):
  - `js-yaml: 4.3.1` — **jediny nosny override**. `@redocly/openapi-core` si rika o **presnou** verzi `4.1.1` (ne caret), takze bez overridu npm nainstaluje zranitelnou 4.1.1. Prechod na js-yaml 5.x neni moznost (redocly 1.x pouziva `JSON_SCHEMA.extend()` + `types.*`, co 5.x nema). Odstranit az `openapi-typescript` vezme major s redocly 2.x — dnes drzi `@redocly/openapi-core: ^1.34.6` a rada 1.x skoncila na 1.34.18.
  - ~~`postcss: 8.5.25`~~ — **odstraneno 2026-08-07.** Override byl zbytecny (vsichni konzumenti maji caret rozsahy) a drzel postcss o patch zpatky; po odstraneni se resolvne 8.5.26. Overeno, ze emitovane CSS je pred i po bajtove identicke.
  - ~~`undici: 7.29.0` a `brace-expansion@2: 2.1.4` / `@5: 5.0.9`~~ — **odstraneno 2026-08-20**, a u `undici` z jineho duvodu, nez tady stalo. `brace-expansion` opravdu jen zbytecne bylo: odstraneni nemeni ve strome ani jeden zaznam. `undici` ale mezitim prestalo byt neskodne — zapis rikal „pin == nejnovejsi verze v rozsahu, ktery by caret vybral sam (`jsdom ^7.25.0`)", jenze `jsdom` 30.0.1 uz zada `undici ^8.9.0`, takze override do nej **potichu vnucoval 7.29.0, cely major mimo deklarovany rozsah** — a jsdom neni nepouzivany, jede na nem devet testovych souboru vcetne `content-sanitizer.test.ts`. Je to tataz trida selhani, pred kterou varuje uvod teto polozky, jen opacnym smerem: pin nepropadl do zranitelneho rozsahu, ale drzel knihovnu pod verzi, kterou jeji konzument vyzaduje. Overeno dvema cistymi rezolucemi lockfilu: 524 balicku v obou, lisi se jediny zaznam (`undici` 7.29.0 → 8.10.0) a `npm audit` je cisty pred i po.
  - `esbuild: 0.28.1` a `cookie: 0.7.2` (pod `@sveltejs/kit`) — nosne, ale jinak: tlaci balicek **nad** deklarovany rozsah konzumenta (`svelte-i18n` chce `esbuild ^0.19.2`, kit chce `cookie ^0.6.0`). Drzet, ale pri kazdem bumpu tech konzumentu overit, ze override neni potreba — a ze porad funguje.
  - Cargo ekvivalent uz vzor ma: [frontend/src-tauri/.cargo/audit.toml](frontend/src-tauri/.cargo/audit.toml) nese u kazdeho ignore removal condition v komentari. Revizi vsech tri sad hlida [backend/RELEASE_CHECKLIST.md](backend/RELEASE_CHECKLIST.md) §8b.

---

## Startup follow-up

Backend (headless) cast zmerena a uzavrena — sekce "Startup audit — mereni 2026-06-11" v [backend/PERFORMANCE_BASELINE.md](backend/PERFORMANCE_BASELINE.md). Zbyva jen GUI:

- [ ] **Manualni startup SR sign-off na packaged buildu (`tauri:build`)** — overit, ze pri objeveni okna cteka cte "načítání". Defenzivni fixy uz hotove (2026-06-30, detail v archive): explicitni polite announce na startu bootu ([bootstrap.ts](frontend/src/lib/bootstrap.ts)) + loading-aware nativni titulek okna (`VoxRox Mail – načítání…` v [lib.rs](frontend/src-tauri/src/lib.rs), prepnut na app name pri boot `ready`/`failed` pres [windowTitle.ts](frontend/src/lib/windowTitle.ts)). Dev SR smoke ukazal, ze spolehlivy kanal pri objeveni okna je titulek, ne prvni aria-live. Gate-relevantni je shell-first "blank screen do 500 ms"; boot timings jsou nice-to-have. Pokud SR smoke ukaze tiche pre-hydratacni okno, doplnit staticky loading text do [app.html](frontend/src/app.html).

---

## Kvalita testu a repo gaty

Vetsina polozek vznikla 2026-08-17 pri odblokovavani dependabot PR a je to
zobecneni uz opravenych konkretnich pripadu, ne novy nalez. Vyjimkou je
`timeout-minutes` v `ci.yml` — ta vzesla ze zaseknute CI pri mergovani #296
(2026-08-19) a je uz opravena, stejne jako apt fetch pri degradovanem mirroru,
ktery po ni zbyl (2026-08-27). Sekce je tim cela zavrena az na dve polozky
frontendu nize.

- [x] Brany na mrtvy kod — HOTOVO 2026-08-24, vsech pet: `-Xdoclint:reference` a `knip:production` (#313), `check:rename-residue` (#314), `check:java-callers` (#316) a `check:test-claims`. Detail ke kazde v [CHANGELOG.md](CHANGELOG.md). Co z toho stoji za pamatovani: (1) „nema volajiciho mimo vlastni soubor" a „je mrtva" jsou dve ruzna tvrzeni a hlasi se zvlast — jinak z prvniho plyne smazani tam, kde patri zmena viditelnosti; (2) brany na sebe navazuji — CodeQL ukaze mrtvy parametr, jeho odstraneni odhali duplicitni test a `check:test-claims` ho pojmenuje; (3) nejzajimavejsi nalezy nebyly mrtvy kod, ale tri testy s `verify(never())` na metodu bez volajicich. **Zbyva jedina vec z te davky:** mutacni testovani (PIT) na „test, ktery nic netvrdi" — zamerne **ne jako brana**, nanejvys obcasny sweep, protoze burn-down by byl vlastni projekt.
- [x] Apt fetch v `Install Tauri system deps` pri degradovanem mirroru — HOTOVO 2026-08-27 (#331): timeout na obe schemata (stall byl na https), rozliseny exit 124/137 od chyby apt, `sleep` uz jen mezi pokusy. Pinnuti mirroru **zamitnuto** — zdroje na tom image jsou mirrorlist a hostitel, na ktery apt prepnul, je ten, ktery se pak zasekl; duvod je v hlavicce [.github/workflows/ci.yml](.github/workflows/ci.yml), detail v [CHANGELOG.md](CHANGELOG.md).
- [x] Stavove automaty `ConversationList` do `.ts` — HOTOVO 2026-08-28: cache clenu vlakna a dvouurovnovy vyber jsou [threadMembers.ts](frontend/src/lib/mail/threadMembers.ts) a [conversationSelection.ts](frontend/src/lib/mail/conversationSelection.ts), obe s vlastni sadou overenou mutacemi. Cil je **obycejne `.ts`, ne `.svelte.ts`** — type-aware blok eslintu ma `ignores: ['**/*.svelte.ts']`, takze runovy soubor by ta pravidla nezapnul, a `SvelteSet`/`SvelteMap` runy nepotrebuji. Odhad −350 radku nevysel (zhruba tretina; objem byly komentare, ktere se stehuji s kodem). Extrakce vydala tri nalezy: `expand()` nove vyzaduje nactene cleny (fetch zamitnuty generacnim citacem uz nenecha radek rozbaleny a prazdny), `refreshExpanded` to potvrzuje znovu misto aby veril loadu, a request nahrazeny invalidaci uz nemaze priznak nacitani sveho nastupce — kvuli cemuz obnova fokusu prestavala cekat o round trip driv a pristala na cizim radku. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] Sdileny radek pro MessageList a SearchResultsGrid — HOTOVO 2026-08-28: [mail-list/](frontend/src/lib/components/mail-list/) nese `MailRow` + pet bunek, obe mrizky je pouzivaji. Sloupce sdilene nejsou (sady se lisi, ARIA indexy jsou konstanty mrizky) — bunka dostava `placement`, a jen ten. `ConversationList` zustava mimo: jeho radek nese treegrid kontrakt a zamerne prazdne bunky, takze `grid-cols-subgrid` na literalu radku zbyva na dvou mistech misto tri. Shoda vykresleneho HTML pred/po dolozena diffem kanonickeho tvaru, ne usudkem. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] Brana na navrat per-radkoveho gridu — HOTOVO 2026-08-20: pravidlo `row-grid-columns` v [check-design.mjs](frontend/scripts/check-design.mjs) odmita `grid-cols-[` na elementu, ktery zaroven nese `role="row"`. **Oproti zadani muselo byt element-scoped, ne radkove:** na skutecnem radku je `role="row"` a ta trida osm radku a jedno `cn(...)` volani od sebe, takze radkove pravidlo by realny pripad nikdy nechytlo. Proto scanner, ne regex — a stopnout se na prvnim `>` taky nejde, protoze `onclick={(e) => …}` ma `>` uvnitr tagu; walk nese stav uvozovek a hloubku zavorek a tag konci az na hloubce nula. Overeno mutacemi, ne jen zelenym behem: s `elementRules = []` padne prave 5 novych testu, s odstranenym sledovanim hloubky padne prave ten o arrow funkci, a proti realnemu stromu se zamerne rozbitym radkem v `MessageList` konci exit 1. Pozor na past: prvni mutace byla vadna (vyprazdnila `allRules`, cimz skript spadl na TypeError misto aby pravidlo vynechal) a 4 testy tim prochazely z nespravneho duvodu.
- [x] `timeout-minutes` v [.github/workflows/ci.yml](.github/workflows/ci.yml) — HOTOVO 2026-08-19: vsech devet jobu ma job-level limit (10 min male joby, 12 `Backend quality`, 35 `Tauri (Rust)`, 25 a 20 testovaci) a tri sitove kroky k tomu vlastni step-level limit (13 apt, 8 stazeni prohlizece) nad bounded retry (200 s na pokus, az 3 pokusy). **Job limit je zaloha proti zatuhlemu behu, ne rozpocet na vykon**, proto sedi vysoko nad stropem uspesnych behu — prvni verze teto zmeny mela `Tauri (Rust)` na 15 a `Functional tests` na 12, coz je **pod** stropem behu, ktere doopravdy prosly (24,0 resp. 15,3 min), a shodilo by je. Hodnoty ze stropu 30 poslednich uspesnych behu, ne z medianu — **a strop byl jinde, nez tahle polozka tvrdila**: `Tauri (Rust)` ma mezi uspesnymi behy maximum 17,4 min, ne ~6, takze zvazovanych ~10 min by utnulo dva behy, ktere doopravdy prosly. Zvazovane `-o Acquire::Retries=3` **nepridano** — log nejpomalejsiho behu konci `Fetched 21.1 MB in 12min 38s (27.8 kB/s)`, tedy pomaly, ale postupujici prenos, na ktery ani `Acquire::Retries` (reaguje na chybu) ani `Acquire::http::Timeout` (na mlceni spojeni) nedosahnou. **Samotny timeout ale nestacil:** prvni beh teto zmeny utnul dva joby po 373 s, cimz se ze „zelene, ale pomale" stala „cervena hned" — proto je pokus ohraniceny _a_ opakovany, zotavuje az to nove spojeni (overeno: Tauri krok utnut na 200 s, druhy pokus zeleny). Pozor na sirotka: `timeout` bezici jako uzivatel nezabije rootovsky `apt-get`, ktereho si pod sebou puste `playwright install --with-deps` — prezije a drzi dpkg zamek. Nakonec ale testovaci joby apt opustily uplne: `playwright install` bezi bez `--with-deps`, ktery na runner image pridava jen fontove baliky (zadnou knihovnu prohlizece) — cimz sirotek prestal mit jak vzniknout a `pkill` se nakonec nemergoval vubec, u Tauri zustal jen bounded retry a `dpkg --configure -a`.
- [x] Brana na eol drift: blob musi odpovidat deklaraci v `.gitattributes` — HOTOVO 2026-08-19: `check:eol` ([check-eol-normalization.mjs](frontend/scripts/check-eol-normalization.mjs)) v `npm run check`. Oproti zadani se ukazalo, ze `git status` nestaci i bez stat cache (blob se smisenymi konci radku hlasi cisty i po `--really-refresh`) a ze brana nesmi soudit pracovni strom — repo ma legitimni `w/crlf` pod `eol=lf`. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] Projit e2e sadu na vzor „`.focus()` a hned klavesa" — HOTOVO 2026-08-18: z 32 mist sady je rizikove jen to, kam appka saha pro fokus asynchronne, a takove je v produkcnim kodu jedine (`MessageContent` parkuje fokus v ramu tela o `requestAnimationFrame` pozdeji). Nalez: `shortcuts.functional.e2e.ts` zaostroval `<main>` hned po otevreni zpravy — **neprojevovalo se to padem**, ram klavesy preposila rodici, takze test prochazel a jen prestal byt pripadem, ktery ma kryt. Mechanismus zmeren (3/3), oprava overena 24/24. Klavesove prochody bits-ui menu zustavaji jen v `row-actions-menu`, kde uz pacing je. Detail v [CHANGELOG.md](CHANGELOG.md).

---

## Produktove funkce (backlog)

Hotove: **Podpisy zprav — Faze 1** (auto-insert + From-swap + manualni tlacitko + per-ucet prepinac, smoke 2026-06-23) a **Tabulka kontaktu** (sloupec Aktualizovano odebran) — detail v [todo-archive.md](todo-archive.md).

- [ ] iCloud OAuth.
- [x] Naseptavac adres z historie korespondence — Faze 1 HOTOVO 2026-08-07: tabulka `correspondent` plnena pri syncu + backfill po startu, `GET /contacts/autocomplete` slucuje adresar a historii se `source: CONTACT | HISTORY`. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] Naseptavac adres z historie — Faze 2 HOTOVO 2026-08-20: radek odesilatele v detailu zpravy nabizi „Pridat do kontaktu" (predvyplneny formular + navrat do zpravy), nebo „Zobrazit kontakt", kdyz uz adresa v adresari je. Do naseptavace to vedome neslo — rozptyluje pri psani a druha akce v polozce rozbije klavesovou navigaci. Detail v [CHANGELOG.md](CHANGELOG.md).
- [ ] **Naseptavac adres z historie — zbyle otazky z Faze 1:** (1) **zmerit realnou velikost `correspondent`** na plne schrance — pokud by slo o rad vic nez nizsi tisice radku na ucet, prestane stacit scan pres `ux_correspondent_account_email` a bude potreba normalizovany sloupec pro shodu podle jmena; (2) smazani vsi posty od nekoho ho necha v naseptavaci, dokud se cache neprepocita (prijatelne, ale je to rozhodnuti — souvisi s tim, jestli ma jit cache invalidovat mazanim).
- [x] Vlastni stitky kontaktu (Google model) — HOTOVO 2026-08-07 (#230): tabulky `contact_labels` + `contact_label_links` misto enumu, sprava stitku + hromadne prirazeni v UI, polozky sidebaru z `GET /contacts/counts`. Enum WORK/HOME/OTHER zustal jako typ adresy. Detail v [CHANGELOG.md](CHANGELOG.md).
- [x] Threading Phase 2 (V0.2) — HOTOVO, vse v `main`: grouped UI (#207/#208/#209) + Outlook-style cross-folder konverzace (#221, merged 2026-08-06). Detail v [todo-archive.md](todo-archive.md), design v [backend/docs/THREADING_DESIGN.md](backend/docs/THREADING_DESIGN.md). Ziva zbyva uz jen polozka NVDA doposlechu vyse.
- [x] Podpisy zprav — Faze 2 — UZAVRENO 2026-08-31 bez zmeny kodu, obe casti odpadly: reply/forward placement resi manualni tlacitko uz z Faze 1 ([signature.ts](frontend/src/lib/compose/signature.ts)) a HTML-compose sanitizace nema co sanitizovat — [MarkdownBodyRenderer](backend/src/main/java/org/voxrox/mailbackend/feature/mail/service/MarkdownBodyRenderer.java) (#239) ma HTML bloky vypnute a inline HTML escapuje, plain `textarea` zustava zamerne (rozhodnuto 2026-08-12, viz Rozhodnuti) a podpis jde stejnou cestou jako telo — drzi to trida `SignatureBlock` v [MarkdownBodyRendererTest](backend/src/test/java/org/voxrox/mailbackend/feature/mail/service/MarkdownBodyRendererTest.java). Detail v [CHANGELOG.md](CHANGELOG.md).
- [ ] **Interactive IMAP lane — druhe spojeni na ucet pro uzivatelske cteni.** Rozhodovaci gate uz neni jen subjektivni beta feedback: metrika **`mail.imap.lock.skipped`** (pridana 2026-08-06) pocita, kolikrat se cteci cesta vzdala kvuli obsazenemu spojeni — rostouci hodnota = jedno spojeni na ucet je bottleneck. **Merena podoba symptomu (2026-08-31, pri NVDA doposlechu):** ohlaseni "Strana X z Y" po prepnuti slozky dorazilo za 5,0 s / 7,0 s / **59,3 s** — visi na `loadPage`, ktery ceka na zamek drzeny bezicim syncem. Ze nejde o objem dat dokazuje kontrolni vzorek: slozka o 12 zpravach na jedne strance byla **pomalejsi** nez slozka o 1801 zpravach. Metriku samotnou pres HTTP precist nejde (`management.endpoints.access.default=none`, vystaveny je jen health). **Rozsah se mezitim zuzil:** vypis konverzaci uz na zamek neceka (`executeWithLockOrSkip`, timeout `mail.client.imap.role-lookup-timeout`), takze zbyva fetch tela a prilohy. **Problem:** jeden pooled `Store` na ucet + fair lock ([ImapConnectionManager.java:105](backend/src/main/java/org/voxrox/mailbackend/feature/mail/service/ImapConnectionManager.java)); sync drzi zamek po celou folder cyklu ([MailSyncService.java:194](backend/src/main/java/org/voxrox/mailbackend/feature/mail/service/MailSyncService.java) — cely `executeInFolder` = download + flag sweep + cleanup, i desitky sekund), takze prvni fetch tela (`MailContentService.getOrFetchMessageContent`) a stazeni prilohy ([AttachmentService.java:78](backend/src/main/java/org/voxrox/mailbackend/feature/mail/service/AttachmentService.java) — cely transfer pod zamkem) cekaji ve FIFO fronte za nim. **Plan:**
  1. `ImapConnectionManager`: enum `Lane {INTERACTIVE, BACKGROUND}`, pool + lock mapy klicovane `(accountId, lane)`; `executeWithLock(accountId, lane, action)`, stavajici signatura deleguje na BACKGROUND (zadna zmena call-sites v kroku 1). Pravidlo "lock entries never removed" (CONCURRENCY.md rule 4) plati per lane; `purgeAccount` + `@PreDestroy` zaviraji obe lane.
  2. Fallback na limit spojeni: kdyz interactive connect selze (server limit soubeznych sessions — Gmail 15 OK, Seznam overit zive), degradovat na BACKGROUND lane + WARN a metrika; chovani pak identicke s dneskem, zadny novy failure mode.
  3. Klasifikace call-sites v `ImapFolderExecutor` (lane parametr): INTERACTIVE = fetch tela (`MailContentService`), prilohy (`AttachmentService`), reply/forward content, lazy page fetch + server count (`fetchServerCountAndEnsurePageLocally` — idempotence uz resi `dropAlreadyPersisted`), cold `getFolders`. BACKGROUND = sync cykly, backfill, maintenance, `ImapActionService` move/flag propagace (lokalni zapis uz je hotovy, serverova strana je eventual), `ImapAppendService` drafty.
  4. OAuth beze zmeny: `refreshLocks` zustava per ucet sdileny pres obe lane (uz dnes serializuje sync vs. SMTP); auth-retry vetev v `executeWithLock` funguje per lane. Lock-order kontrakt rozsirit: nikdy nedrzet zamky obou lane naraz; poradi SyncLockManager → connection lock (jedne lane) → refresh lock zustava.
  5. Docs + testy: radky v [backend/docs/CONCURRENCY.md](backend/docs/CONCURRENCY.md) (inventar + lock order), ArchitectureTest beze zmeny (Store zustava ve `feature.mail.service`). Latch-style GreenMail IT dokazujici invariant "fetch tela dobehne, zatimco BACKGROUND lane drzi bezici sync" (vzor `MailSyncGreenMailIT`); unit testy na lane routing + fallback z kroku 2. Pred releasem 24h soak s JFR (lock contention obou lane).
  6. Rizika k overeni v IT: expunge race (INTERACTIVE cte zpravu, kterou BACKGROUND prave mazal → musi skoncit fail-soft `contentError`, ne 500); UIDVALIDITY reset pozorovany jen sync lane (read path uz je stub-tolerantni). Odhad ~1 den vc. testu. Levnejsi nouzovka, kdyby limit spojeni branil druhe lane: rozsekat sync cyklus na kratsi `executeWithLock` useky (fair lock pusti cekajici mezi davkami) za cenu SELECT overheadu a peclivosti na invariant "local = souvisle nejnovejsi okno".
- `OpenApiSnapshotTest` nedeterminismus — `canonicalOpenApi` rekurzivne radi klice pred zapisem i porovnanim, snapshot i `schema.d.ts` jednorazove preskladany (2026-08-07). Overeno trema po sobe jdoucimi update behy (bajtove identicke) + ostrym behem.
- [ ] Full QRESYNC SELECT s VANISHED — vetsi refactor `ImapFolderExecutor`; po release, pokud bude cleanup latency bottleneck.
- [ ] Self-update standalone backendu (jen pokud vznikne deployment mimo Tauri bundle).
- [ ] Dlouhodobe sledovat startup performance a velikost bundle.
- [ ] Tauri release smoke s AOT cache (perf, NE release blocker) — `npm run tauri:build:with-sidecar` s `$EnableAotCache=$true`, zmerit desktop `appReady` cold start a zapsat do [backend/PERFORMANCE_BASELINE.md](backend/PERFORMANCE_BASELINE.md). Backend cold start uz je v gate (3,7 s); tohle je optimalizacni cislo, ne blocker.

---

## Dev spousteni

```powershell
cd C:\dev\java\mail\frontend
npm run tauri:dev
```

Po zmene backendu (prebalit + zkopirovat sidecar):

```powershell
cd C:\dev\java\mail\backend
.\scripts\package-sidecar-dev-windows.ps1 -SkipTests
cd C:\dev\java\mail\frontend
npm run sidecar:sync:windows
npm run tauri:dev
```

`package-sidecar-dev-windows.ps1` je dev wrapper: nacte OAuth hodnoty z `backend/.env` a zavola `scripts/package-sidecar-windows.ps1`, takze lokalni build ma funkcni OAuth login. Cisty `scripts/package-sidecar-windows.ps1` cte jen prostredi (pro CI, kde secrets jdou z env) — bez nastavenych promennych **skonci chybou**, placeholder zabali jen kdyz mu das `-AllowPlaceholderOAuth` (fail-closed, aby se nedal omylem vydat build s nefunkcnim OAuth loginem).

---

## Pre-Push Quality Gate

Seznam gate je v [CONTRIBUTING.md](CONTRIBUTING.md) „Pre-Push Quality Gate" —
jediny zdroj pravdy. Zdejsi kopie se od nej rozesla (chybel v ni `npm run lint`,
tedy prettier + `check:md` + eslint + `check:i18n`, ktery pre-push hook realne
pousti jako prvni krok), takze se nahrazuje odkazem misto aby se dorovnavala.

Zbyva tady jen to, co v CONTRIBUTING neni:

- Po `git add .` rucne projit staged soubory.
- Git hooky: `git config core.hooksPath .githooks` (per clone).

- [ ] **Brana na pomalejsim stroji: ~50 minut se nereprodukuje.** Zmereno 2026-08-13 primo
      na tom stroji (i7-1255U, 15W U-series, 1,7 GHz base, 16 GB, NVMe, plan Rovnovaha),
      kroky jednotlive: cela brana 106 s. Nejdrazsi je `lint` 34,5 s a `test:unit` 27,4 s,
      zbytek `svelte-check` 10,8 s, `check:audits` 7,5 s, `svelte-kit sync` 6,7 s, `knip`
      6,6 s, ostatni `check:*` pod 3,5 s. Se starou konfiguraci pred #264 (`projectService`
      nad `.svelte`, coz je stav, ve kterem tech 50 minut bezelo) vychazi `lint` 120,6 s,
      tedy cela brana ~192 s — ani to na 50 minut nestaci, chybi faktor ~15.
      Vylouceno: `svelte-check` (desetina toho, co `lint`), Defender (na tom stroji vubec
      nebezi, `WinDefend` je Stopped a real-time dela ESET) i sit (brana je cela offline).
      Pricina tedy neni v repu. Stopa, kterou to merenim vydalo: cisla na tom stroji
      **kolisaji mezi behy** — druhy pruchod dal `lint` 19,4 s proti 34,5 s prvniho a
      celou branu 64 s proti 106 s, pri identickem prikazu i stromu. Propustnost stroje
      tedy neni stabilni (15W CPU, termalni skrceni), coz je presne ten typ chovani, ktery
      se za horsich podminek muze nasobit. Kdyby se to vratilo, zmerit **za behu** toho
      pomaleho pruchodu, ne zpetne — podezreli zustavaji jednorazove vlivy: soubezna zatez stroje,
      planovany sken ESETu, prvni pruchod pres cerstve `node_modules` po `npm ci` (ESET
      skenuje desitky tisic souboru pri prvnim dotyku), beh na baterii a termalni skrceni
      15W CPU. Stejne tak je mozne, ze „50 minut" byl wall-clock cele push session vcetne
      selhani a oprav, ne jeden pruchod branou.

---

## Definition of Done

- Polozka se odskrtava az po realnem overeni.
- Nove ukoly maji byt akcni, s jasnym vysledkem.
- Hotove implementacni deniky se do tohoto souboru nepridavaji — patri do commitu, changelogu nebo dokumentace. Hotovou polozku zkratit na jeden radek + pripadne presunout detail do [todo-archive.md](todo-archive.md).
- U zmen backendu pro Tauri nezapomenout prebalit a zkopirovat sidecar.
