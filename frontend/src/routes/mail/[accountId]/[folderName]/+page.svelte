<!--
	Title is set in the parent +layout.svelte via the localised
	`folderLabel`. Nothing renders here — the folder contents are rendered
	by MailSidebar + MessageList from the layout, and the page-level
	default state is handled there too.
-->
